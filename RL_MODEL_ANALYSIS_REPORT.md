# 📊 UAV 통신 장애 복구 RL 모델 구현 분석 보고서

**작성 목적:** 현재 대시보드(`dashboard.py`) 및 RL 모델(`rl_position_correction.py`)에 구현된 위치 보정 로직의 한계를 분석하고, 향후 고도화(PPO 도입 등)를 위한 설계 방향을 점검함.

---

## 1. 강화학습 상태(State) 변수에 SNR 등 통신 지표 포함 타당성 검토

**결론: 상태(State) 변수에 RSSI 외에 SNR(신호 대 잡음비) 등 추가 통신 지표를 포함해야 한다는 의견은 전파 통신의 물리적 특성을 고려할 때 매우 타당함.**

*   **현재 코드의 구현 상태:** 
    현재 `_dqn_correction` 함수(대시보드)와 RL 환경(RL 환경 코드)에서 정의된 8차원 State는 다음과 같습니다.
    `[상대 X거리, 상대 Y거리, 유클리드 거리, 최근접 UAV의 RSSI, 최근접 거리, 스텝 수, 장애물 차폐 여부, 초기 RSSI]`
    통신 품질 지표로는 **RSSI(수신 신호 강도)만 고려**되고 있습니다.

*   **논리적 타당성 분석:**
    수신 신호 강도(RSSI)가 높다고 하더라도, 도심 환경(건물 반사, 간섭 등)으로 인해 노이즈(Noise)가 함께 커질 수 있습니다. 즉, 실제 패킷을 성공적으로 해독하고 통신을 유지할 수 있는지 결정하는 핵심 지표는 단순 신호 강도가 아닌 **SNR(신호 대 잡음비)**입니다. RSSI만 보고 이동할 경우, 노이즈가 심한 지역으로 드론이 잘못 이동할 위험이 존재합니다.

*   **개선 방향:**
    실제 프로젝트의 예측 모델(LSTM, Transformer)들은 이미 7개의 주요 피처(`rssi_dbm_est`, `snr_db_est`, `plr_pct_est`, `throughput_mbps_est` 등)를 사용하여 높은 성능을 달성하고 있습니다. 
    강화학습 모델의 신뢰도를 높이기 위해, 향후 모델 구조 개편 시 **State Space에 SNR과 PLR(패킷 손실률) 지표를 반드시 추가**해야 합니다.

---

## 2. RL 기반 위치 보정의 이동 거리 결정 방식 분석

**결론: 현재 RL 모델(DQN)의 보정 이동 거리는 10m로 고정되어 있지 않으며, 모델이 4가지 후보 거리 중 하나를 선택하도록 설계되어 있음.**

*   **이동 거리 선택 로직 (Discrete Action):**
    코드 내에 정의된 상수들을 확인해보면 이동 거리는 다음과 같이 분할된 이산적(Discrete) 공간을 가집니다.
    *   방향: `_DQN_DIRECTIONS` = 8방향 (45도 간격)
    *   거리: `_DQN_STEP_SIZES` = `[10.0, 20.0, 30.0, 40.0]` (단위: m)
    현재 모델은 이 조합으로 생성된 **총 32개의 행동 중 하나를 출력**합니다. 즉, 수식으로 고정된 것이 아니라 상황에 맞춰 10m를 이동할지, 40m를 이동할지 모델이 스스로 선택합니다.

*   **Baseline (수식 기반) 이동과의 오해 요인:**
    수식으로 거리가 고정되었다고 헷갈릴 수 있는 부분은, RL 모델을 사용하지 않는 Baseline 로직(단순 Heuristic)입니다. Baseline은 중심점(Centroid) 방향을 구한 뒤 상수로 지정된 `STEP_ARROW = 20`을 이용해 **항상 20m 거리를 고정적으로 이동**하도록 수식화되어 있습니다.

*   **개선 방향 (PPO 도입의 필요성):**
    DQN은 구조적으로 연속적인 출력값을 가질 수 없어 `[10, 20, 30, 40]`과 같이 끊어진 값만 낼 수 있습니다. 
    드론이 13.5m, 27.2m처럼 상황에 맞는 **부드럽고 연속적인(Continuous) 거리를 이동하도록 고도화하려면, 현재의 DQN 모델을 PPO(Proximal Policy Optimization) 모델로 교체**하는 것이 최적의 설계 방향입니다.

---

## 💡 최종 종합 제언
1. **관측(State) 개선:** 통신 노이즈를 반영할 수 있도록 `SNR`, `PLR` 지표를 State Vector에 추가.
2. **행동(Action) 개선:** 이산화(Discrete)된 방향과 거리 제어의 한계를 극복하기 위해, 모델을 **PPO 기반의 연속 행동 공간(Continuous Action Space)**으로 재설계하여 수식적이고 정밀한 위치 보정 구현.