# 🚀 UAV 강화학습 모델 고도화를 위한 코드 수정 가이드 및 프롬프트

## 1. 프로젝트 핵심 개선 포인트 요약

1. **PPO (1단계: 중계 노드 선택) 완벽 통합 및 상태(State) 확장**
   * **현재:** `ml_server.py`에서 단순 RSSI 합산 휴리스틱(`_select_relay`)을 사용 중.
   * **개선:** PPO 모델을 로드하여 추론하도록 서버 코드를 수정. 이때 상태 벡터(State Vector)에 RSSI뿐만 아니라 도심 환경의 노이즈를 반영하기 위해 **SNR(신호 대 잡음비)**과 **PLR(패킷 손실률)**을 반드시 포함해야 함.

2. **DQN (2단계: 물리적 위치 보정) 상태(State) 차원 확장**
   * **현재:** 8차원 상태 (거리, 중심점, 최근접 RSSI, 차폐 여부 등) 사용.
   * **개선:** 10차원 이상으로 확장하여 **최근접 UAV와의 SNR 및 PLR** 지표를 추가. 이를 통해 "신호는 세지만 노이즈가 심해서 통신이 안 되는 방향"으로 드론이 잘못 이동(배터리 낭비)하는 것을 방지.

3. **NS-3 실시간 통신 파이프라인(C++ ↔ Python) 데이터 보강**
   * **현재:** `uav-adhoc-logging.cc`가 Python 서버로 JSON을 보낼 때 `rssi`만 포함하여 전송 중.
   * **개선:** C++ 코드의 `BuildJson` 함수에서 링크별 `snr`과 `plr` 정보를 추가로 계산(또는 추정)하여 JSON에 담아 보내도록 수정해야 함.

---

## 2. Claude에게 전달할 최적의 프롬프트 (Copy & Paste)

```text
현재 UAV 군집 통신 장애 복구를 위한 2단계 강화학습 아키텍처(1단계 PPO 중계 선택, 2단계 DQN 위치 보정)를 구현 중이야.
기존 코드의 한계를 극복하기 위해 아래 3가지 주요 개선 사항을 반영해서 코드를 전체적으로 수정해줘.

[도메인 지식 배경]
도심 환경(빌딩 반사, 간섭 등)에서는 수신 신호 강도(RSSI)가 높아도 노이즈나 패킷 손실이 클 수 있어. 따라서 강화학습 에이전트가 정확한 판단(특히 드론의 물리적 이동 방향 결정)을 내리려면 State에 RSSI뿐만 아니라 SNR(신호 대 잡음비)과 PLR(패킷 손실률)이 반드시 포함되어야 해.

[수정 요청 사항]
1. `uav-adhoc-logging.cc` (NS-3 C++ 코드) 수정:
   - `BuildJson` 함수에서 Python 서버로 링크 상태를 보낼 때, 현재의 `rssi` 외에 `snr`과 `plr` 값도 포함해서 직렬화(JSON)하도록 추가해줘. (간단한 수식 기반 추정치로 구현해도 좋음)

2. `ml_server.py` (Python 서버 코드) 수정:
   - NS-3에서 보내온 JSON 데이터 파싱 부분에 `snr`, `plr`을 받도록 업데이트해줘.
   - 기존의 `_select_relay()` (단순 RSSI 합 휴리스틱) 로직을 제거하고, 사전 학습된 PPO 모델(`ppo_relay_model.zip` 가정)을 로드하여 Relay를 선택하도록 코드를 수정해줘.
   - PPO 모델의 입력 State를 구성할 때 링크별 RSSI, SNR, PLR을 모두 포함하도록 반영해줘.

3. `rl_position_correction.py` 및 `ml_server.py`의 DQN 로직 수정:
   - DQN 모델의 입력 State 차원을 기존 8차원에서 10차원(또는 그 이상)으로 확장해줘.
   - 추가되는 State 변수는 '가장 가까운 메인 군집 UAV와의 SNR'과 'PLR'이야.
   - 이에 맞춰 PyTorch 신경망(`_Net` 및 `DQN` 클래스)의 `nn.Linear` 입력 차원(input_dim)을 업데이트해줘.

[출력 요구 사항]
- 수정해야 할 핵심 파일(`uav-adhoc-logging.cc`, `ml_server.py`, `rl_position_correction.py`)들의 변경된 코드를 제공해줘.
- 코드는 부분 수정이 아닌, 교체하기 쉽도록 주요 함수나 클래스 단위로 완성해서 보여줘.
```